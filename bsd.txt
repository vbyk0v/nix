0) ee /etc/group
    wheel:*:0:root,user
    video
    operator

1)sudo:
  pkg install sudo
  visudo or:
  ee /usr/local/etc/sudoers:
    wheel        ALL=(ALL) ALL
    wheel ALL=(ALL) NOPASSWD: ALL
    
2)update:
	2.2) update sec:
	freebsd-update fetch + (vi:q)
	freebsd-update install
	freebsd-update rollback - gone wrong
	
	2.3)update pkg:
	pkg update
	
3)install software pkg:
	pkg install packet
	pkg search packet
	pkg info packet
	pkg delete packet

4)virtualbox:
pkg install virtualbox-ose virtualbox-ose-additions

ee /etc/rc.conf >>
vboxguest_enable="YES"
vboxservice_enable="YES"

5)FQDN
ee /etc/rc.conf >> hostname = "hostname.domain.com"

6) pkg install xorg xf86-video-intel xfce xfce4-goodies slim slim-themes
	ee /etc/rc.conf >>	
	hald_enable="YES"
	dbus_enable="YES"
	moused_enable="YES"
	slim_enable="YES"
	
	ee /usr/local/etc/slim.conf
	current_theme       fbsd
6.1) https://forums.freebsd.org/threads/how-to-use-the-old-or-the-new-i915kms-driver-for-intel-integrated-graphics-with-xorg.66732/

7) cp /usr/local/etc/xdg/xfce4/xinitrc ~/.xinitrc
ee ~/.xinitrc:
	export LANG="ru_RU.UTF-8"
	export LC_TYPE="ru_RU.UTF-8"
8) mkdir ~/.icons
    mkdir ~/.themes
9) cp font.ttf /usr/local/share/fonts/TTF
