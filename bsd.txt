0)useradd wheel or:
  ee /etc/group
    wheel:*:0:root
    wheel:*:0:root,user

1)sudo:
  pkg install sudo
  visudo or:
  ee /usr/local/etc/sudoers:
    wheel        ALL=(ALL) ALL
    wheel ALL=(ALL) NOPASSWD: ALL
    
2)update:
	2.2) update sec:
	freebsd-update fetch + (vi:q)
	freebsd-update install
	freebsd-update rollback - gone wrong
	
	2.3)update pkg:
	pkg update
	
3)install software pkg:
	pkg install packet
	pkg search packet
	pkg info packet
	pkg delete packet
	
3.1)change mirror
ee /etc/pkg/FreeBSD.conf 
or
ee /usr/local/etc/pkg.conf

>>pkg.eu.FreeBSD.org

4)virtualbox:
pkg install virtualbox-ose virtualbox-ose-additions

ee /etc/rc.conf >>
vboxguest_enable="YES"
vboxservice_enable="YES"

4.1) https://kb.vmware.com/s/article/2149806
	For a desktop system (with X11) :  pkg install open-vm-tools
	For a server system (no X11) :  pkg install open-vm-tools-nox11  
	/usr/local/etc/rc.d/vmware-guestd start


5)FQDN
ee /etc/rc.conf >> hostname = "hostname.domain.com"

6) terminal resolution
	#vidcontrol -i mode
	#vidcontrol MODE_X
/etc/rc.conf
	vesa_load="YES"
	allscreens_flags="MODE_X"
	
8) xorg + xfce + slim
	pkg install xorg xfce slim
	
	ee /etc/rc.conf >>	
	hald_enable="YES"
	dbus_enable="YES"
	slim_enable="YES"
	
	cp /usr/local/etc/xdg/xfce4/xinitrc ~/.xinitrc
	
	
9)	Edit /usr/local/etc/slim.conf, set theme:

	pkg install slim-themes
	
	/usr/local/share/slim/themes
	
	#current_theme       default
	current_theme       fbsd
10) intel drivers
	pkg install xf86-video-intel
	ee /usr/local/etc/X11/xorg.conf.d/driver-intel.conf
	
	Section "Device"
		Identifier "Card0"
		Driver "intel"
	EndSection

11) burn iso
	pkg install pv
	fdisk -BI /dev/da0
	newfs_msdos /dev/da0sl
	pv /home/disk.iso > /dev/sda0s1
	
12) change mirror
ee /etc/pkg/FreeBSD.conf 
or
ee /usr/local/etc/pkg.conf

>>pkg.eu.FreeBSD.org
