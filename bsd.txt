0)useradd wheel or:
  ee /etc/group
    wheel:*:0:root
    wheel:*:0:root,user

1)sudo:
  pkg install sudo
  visudo or:
  ee /usr/local/etc/sudoers:
    wheel        ALL=(ALL) ALL
    wheel ALL=(ALL) NOPASSWD: ALL
    
2)update:
	2.2) update sec:
	freebsd-update fetch + (vi:q)
	freebsd-update install
	freebsd-update rollback - gone wrong
	
	2.3)update pkg:
	pkg update
	
3)install software pkg:
	pkg install packet
	pkg search packet
	pkg info packet
	pkg delete packet
	
3.1)change mirror
ee /etc/pkg/FreeBSD.conf 
or
ee /usr/local/etc/pkg.conf
>>pkg.eu.FreeBSD.org
pkg update

4)virtualbox:
pkg install virtualbox-ose virtualbox-ose-additions

ee /etc/rc.conf >>
vboxguest_enable="YES"
vboxservice_enable="YES"

5)FQDN
ee /etc/rc.conf >> hostname = "hostname.domain.com"

6) pkg install xorg openbox obconf obmenu openbox-themes slim slim-themes
	ee /etc/rc.conf >>	
	hald_enable="YES"
	dbus_enable="YES"
	slim_enable="YES"
	
	ee /usr/local/etc/slim.conf
	current_theme       fbsd
	
	$ ee ~/.xinitrc
	exec openbox-session
	
7) obconf
	mkdir ~/.config/openbox
	cp /usr/local/etc/xdg/openbox/menu.xml ~/.config/openbox
